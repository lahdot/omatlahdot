<%= form_for(@reittiopa) do |f| %>
  <% if @reittiopa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reittiopa.errors.count, "error") %> prohibited this reittiopa from being saved:</h2>

      <ul>
      <% @reittiopa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :query %><br />
    <%= f.text_field :query %>
  </div>
  <div class="field">
    <%= f.label :result %><br />
    <%= f.text_area :result %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
